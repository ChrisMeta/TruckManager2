@tailwind base;
@tailwind components;
@tailwind utilities;

.leaflet-container{width:100%;height:100%;z-index: 1}
.card{@apply bg-card/90 rounded-2xl shadow-soft p-4 border border-white/5}
.btn{@apply px-3 py-2 rounded-xl border border-white/10 hover:bg-white/10 active:scale-95 transition}
.btn-primary{@apply bg-accent text-black font-semibold hover:brightness-110 border-none}
.input{@apply w-full px-3 py-2 rounded-xl bg-panel/90 border border-white/10 focus:outline-none focus:ring-2 focus:ring-accent/50}
.badge{@apply text-xs px-2 py-0.5 rounded-full bg-white/10 border border-white/10}
.h{@apply text-lg font-semibold}
.sub{@apply text-sm text-gray-400}

/* NEW for tabbed UI */
.tab{@apply px-3 py-2 rounded-xl hover:bg-white/10}
.tab-active{@apply bg-white/10}

/* Mobile menu button - force to front */
.mobile-menu-btn {
  z-index: 9999 !important;
  position: fixed !important;
}

/* Ensure Leaflet map doesn't interfere */
.leaflet-control-container {
  z-index: 1000;
}

/* Responsive utilities */
@media (max-width: 768px) {
  .tab {
    @apply px-2 py-1.5 text-sm;
  }
}

/* Resize handle styles */
.resize-handle {
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 4px;
  cursor: ew-resize;
  z-index: 10;
  transition: background-color 0.2s ease;
}

.resize-handle:hover {
  background-color: rgba(124, 140, 255, 0.5);
}

.resize-handle.resizing {
  background-color: rgba(124, 140, 255, 1);
}

/* Prevent text selection during resize */
.no-select {
  user-select: none;
  -webkit-user-select: none;
  -moz-user-select: none;
  -ms-user-select: none;
}

/* Ensure content doesn't overflow on small screens */
.side-panel-content {
  word-wrap: break-word;
  overflow-wrap: break-word;
}

/* Mobile-specific styles */
@media (max-width: 768px) {
  .mobile-full-screen {
    @apply fixed inset-0 z-40;
  }
}

/* Smooth transitions for panel resizing */
.panel-transition {
  transition: width 0.1s ease-out;
}